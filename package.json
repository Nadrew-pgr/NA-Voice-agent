{
  "name": "assistant-ia-notes",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
